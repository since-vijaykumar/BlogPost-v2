<%- include('partials/header'); -%>

<h1> <%= home %></h1>

<p> <%= homePageContents %> </p>

<%  posts.forEach( (post) => { %>
<h1> <%= post.title; %></h1>
<p class="paragraph"> <%= (post.content.substring(0, 100) + "...") ; %><a href="/post/<%= post._id%>">Read More</a></p>
<% }) %>

<%- include('partials/footer'); -%>
